<%= form_for @project do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %><br>

    <%= f.label :description %>
    <%= f.text_area :description %><br>

    <%= f.label :outcrop %>
    <%= f.collection_select :outcrop_ids, Outcrop.all, :id, :location, {include_blank: true}  %>
    <br>


        <br>
    <%= f.submit %>

<% end %>